# Servidor Flask para Raspberry Pi 3
# Dependências otimizadas para hardware limitado

Flask>=2.3.0
gunicorn>=21.0.0
Flask-JWT-Extended>=4.5.0
Flask-Bcrypt>=1.0.1
bcrypt>=4.0.0

# OpenCV headless (sem GUI) - versão básica
# O LBPH FaceRecognizer está incluído no opencv-python-headless
# NÃO usar opencv-contrib-python-headless (muito grande para compilar no Pi)
opencv-python-headless

# Numpy - instalado automaticamente pelo OpenCV
numpy
